# Самопроверка — M1-W02

## Автоматическая
- [ ] `pytest -q` — все тесты зелёные.
- [ ] тесты НЕ ходят в сеть (отключи интернет/ключ — тесты всё равно проходят).
- [ ] `uvicorn src.main:app` стартует, `/docs` открывается.

## Тест «с чистого листа» (45 мин)
Не подглядывая, подними новый FastAPI-сервис с `/health` и одним POST-эндпоинтом, который принимает Pydantic-модель и возвращает Pydantic-модель, и напиши 1 тест через TestClient. Сделал без подсказок -> score 3; объясняешь каждую часть -> score 4.

## Защита решения
В `solution-notes.md`: «Почему вызов LLM вынесен в отдельный модуль `llm_client`, а не написан прямо в эндпоинте?» (ответ про тестируемость и смену провайдера).
