# Самопроверка — M1-W01

## Автоматическая
- [ ] `python experiments/first_llm_call.py` отрабатывает без ошибок и создаёт `outputs/first_response.json`.
- [ ] `git status` не показывает `.env` (секрет не утёк).
- [ ] `grep -ri "LLM_API_KEY=sk" .` ничего не находит в коммите (нет хардкода ключа).

## Тест «с чистого листа» (главный)
Засеки 30 минут. Не подглядывая в готовый код, заново напиши скрипт, который:
1. читает ключ из `.env`;
2. шлёт один промпт в модель;
3. печатает только `answer` и число токенов.
Если смог — навык усвоен (self-score 3). Если можешь ещё и объяснить каждую строку коллеге — score 4.

## Защита решения (1 вопрос недели)
Ответь письменно в `solution-notes.md`: «Почему я выбрал такую temperature по умолчанию для будущего gateway и в каких задачах её надо менять?»
